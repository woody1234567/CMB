[project]
name = "vercel-fastapi-starter"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "camb>=1.6.5",
  "fastapi>=0.121.0",
  "uvicorn>=0.34.0",
]

[tool.vercel.app]
entrypoint = "main.py"
